import { GuestCheckoutView } from "@/components/checkout/GuestCheckoutView";

export const metadata = {
  title: "Guest Checkout",
};

export default function GuestCheckoutPage() {
  return <GuestCheckoutView />;
}
